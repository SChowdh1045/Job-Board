/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{ hostname: "zy4omnrsqut3lcuj.public.blob.vercel-storage.com", }],
    },
}

module.exports = nextConfig
